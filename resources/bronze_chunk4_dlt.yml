resources:
  pipelines:
    pipeline_chunk4_dlt:
      name: chunk4_dlt
      libraries:
        - notebook:
            path: ../src/bronze/06_bronze_dlt.py
      schema: ${var.bronze_schema}
      catalog: ${var.catalog}
      serverless: true