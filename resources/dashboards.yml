resources:
    dashboards:
        d1:
            display_name: Market_Intelligence_Performance
            file_path: ../src/Dashboard/Market_Intelligence_Performance.lvdash.json
            warehouse_id: ${var.sql_warehouse}
            dataset_catalog: ${var.catalog}
            dataset_schema: ${var.gold_schema}
            embed_credentials: false 
        
        d2:
            display_name: Car_Market_Analytics
            file_path: ../src/Dashboard/Car_Market_Analytics.lvdash.json
            warehouse_id: ${var.sql_warehouse}
            dataset_catalog: ${var.catalog}
            dataset_schema: ${var.gold_schema}
            embed_credentials: false