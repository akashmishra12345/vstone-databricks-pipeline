name: Databricks Validation & Testing

on:
  push:
    branches: dev

env:
  DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
  DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
  DATABRICKS_SERVERLESS_COMPUTE_ID: "auto"

permissions:
  contents: read

jobs:
  # 1. Validate Bundle Configuration
  validate-bundle:
    name: Validate Bundle Configuration
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Install Databricks CLI
        uses: databricks/setup-cli@main
      - name: Validate Databricks Bundle
        run: databricks bundle validate

  # # # 2. Run Bronze Unit Tests
  # run-bronze-tests:
  #   name: Run Bronze Unit Tests
  #   runs-on: ubuntu-latest
  #   needs: validate-bundle
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #     - name: Set up Python
  #       uses: actions/setup-python@v4
  #       with:
  #         python-version: "3.10"
  #     - name: Install dependencies
  #       run: |
  #         pip install --upgrade pip
  #         pip install pytest databricks-connect
  #     - name: Run Bronze tests
  #       run: pytest tests/bronze_testing -v --maxfail=1 --disable-warnings

  # # 3. Run Silver Unit Tests
  # run-silver-tests:
  #   name: Run Silver Unit Tests
  #   runs-on: ubuntu-latest
  #   needs: validate-bundle
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #     - name: Set up Python
  #       uses: actions/setup-python@v4
  #       with:
  #         python-version: "3.10"
  #     - name: Install dependencies
  #       run: |
  #         pip install --upgrade pip
  #         pip install pytest databricks-connect
  #     - name: Run Silver tests
  #       run: pytest tests/silver_testing -v --maxfail=1 --disable-warnings

  # # # 4. Run Gold Unit Tests
  # run-gold-tests:
  #   name: Run Gold Unit Tests
  #   runs-on: ubuntu-latest
  #   needs: validate-bundle
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #     - name: Set up Python
  #       uses: actions/setup-python@v4
  #       with:
  #         python-version: "3.10"
  #     - name: Install dependencies
  #       run: |
  #         pip install --upgrade pip
  #         pip install pytest databricks-connect
  #     - name: Run Gold tests
  #       run: pytest tests/gold_testing -v --maxfail=1 --disable-warnings

  5. Integration Tests
  run-integration-test:
    name: Run Integration Test
    runs-on: ubuntu-latest
    needs: [run-bronze-tests, run-silver-tests, run-gold-tests]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install pytest databricks-connect
      - name: Run integration test
        run: pytest tests/integration_test -v --maxfail=1 --disable-warnings

  # # 6. Deploy and Notify
  # deploy-and-run:
  #   name: Deploy and Run Jobs
  #   runs-on: ubuntu-latest
  #   needs: run-integration-test
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #     - name: Install Databricks CLI
  #       uses: databricks/setup-cli@main
  #     - name: Deploy Bundle
  #       run: databricks bundle deploy --target dev
  #     - name: Run Databricks Job
  #       run: databricks bundle run end_to_end_pipeline --target dev

      # # ========================================================================
      # # EMAIL NOTIFICATION (Using SMTP)
      # # ========================================================================
      # - name: Send Deployment Email
      #   if: success()
      #   uses: dawidd6/action-send-mail@v3
      #   with:
      #     server_address: smtp.gmail.com 
      #     server_port: 465
      #     username: ${{ secrets.MAIL_USERNAME }}
      #     password: ${{ secrets.MAIL_PASSWORD }}
      #     subject: " Databricks Deployment Successful: ${{ github.repository }}"
      #     to: akash.r.mishra@v4c.ai
      #     from: "GitHub Actions CI/CD"
      #     body: |
      #       Deployement complemented 

      #       Databricks pipeline 'end_to_end_pipeline' successfully deployed.
            
      #       Details:
      #       - Repository: ${{ github.repository }}
      #       - Branch: ${{ github.ref_name }}
      #       - Commit: ${{ github.sha }}
      #       - Run Link: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}